namespace CinemaBooking.Enum
{
    public enum TicketStatus
    {
        Active = 1,       // Ticket is active or confirmed
        Cancelled = 2,    // Ticket was cancelled
        Used = 3,         // Ticket has been used
        Pending = 4       // Ticket is pending or awaiting confirmation
    }
}
